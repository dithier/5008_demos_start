#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main() {
    // create pid_t variable

    // initialize x

    // fork and have child call ++x
    // have parent call --x

    return 0;
}
